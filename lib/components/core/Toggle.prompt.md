One-line: The PILOT settings switch — cyan glow when on, muted navy track when off.

```jsx
<Toggle checked={sfx} onChange={setSfx} />
```

Controlled. Pair with an uppercase label + icon row (volume_up / music_note) as in the settings panel.
