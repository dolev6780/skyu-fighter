One-line: The cyan-ringed pilot avatar from the top bar.

```jsx
<Avatar />
<Avatar initials="MV" />
<Avatar src="pilot.png" size={56} />
```

Falls back image → initials → `person` glyph. Recolor the ring with `ring` to match a jet skin if desired.
